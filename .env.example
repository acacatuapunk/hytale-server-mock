# Configuração do Servidor Hytale Mock

# Porta do servidor (padrão: 5520)
PORT=5520

# Host do servidor (padrão: localhost)
HOST=localhost

# Ambiente (development/production)
NODE_ENV=development

# Número máximo de jogadores (padrão: 10)
MAX_PLAYERS=10

# Nome da world/zona
WORLD_NAME=Zone 1
